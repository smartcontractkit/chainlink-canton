# Ceremony state — commit via chainlink-canton-ceremonies, not chainlink-canton
ceremonies/
**/workflow.json
**/reports.json
participant-config.json

# Operator-local node configs (JWTs, KMS ARNs — refresh before each run)
config/testnet/nodes/
config/mainnet/nodes/
config/staging/nodes/

/bin
