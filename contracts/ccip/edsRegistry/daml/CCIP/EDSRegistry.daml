module CCIP.EDSRegistry where

import MCMS.RawInstanceAddress (assertValidInstanceId)

template ContractRegistration
    with
        instanceId : Text
        owner : Party
        
        ccipOwner : Party
        
        edsBaseUrl : Text
    where
        signatory owner
        observer ccipOwner
        ensure assertValidInstanceId instanceId
