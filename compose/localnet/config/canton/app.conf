# re-used storage config block
_storage {
  type = postgres
  config {
     dataSourceClass = "org.postgresql.ds.PGSimpleDataSource"
     properties = {
       serverName = ${?DB_SERVER}
       portNumber = 5432
       databaseName = participant
       currentSchema = participant
       user =  ${?DB_USER}
       password = ${?DB_PASSWORD}
       tcpKeepAlive = true
     }
   }
   parameters {
     max-connections = 32
     migrate-and-start = true
   }
 }

canton {
  features {
    enable-preview-commands = yes
    enable-testing-commands = yes
  }
  parameters {
    manual-start = no
    non-standard-config = yes
    # Bumping because our topology state can get very large due to
    # a large number of participants.
    timeouts.processing.verify-active = 40.seconds
    timeouts.processing.slow-future-warn = 20.seconds
  }

  # Bumping because our topology state can get very large due to
  # a large number of participants.
  monitoring.logging.delay-logging-threshold = 40.seconds
}


_participant {
  init {
    generate-topology-transactions-and-keys = false
    identity.type = manual
  }

  monitoring.grpc-health-server {
    address = "0.0.0.0"
    port = 5061
  }
  storage = ${_storage}

  admin-api {
    address = "0.0.0.0"
    port = 5002
  }

  init.ledger-api.max-deduplication-duration = 30s

  ledger-api {
    # TODO(DACH-NY/canton-network-internal#2347) Revisit this; we want to avoid users to have to set an exp field in their tokens
    max-token-lifetime = Inf
    # Required for pruning
    admin-token-config.admin-claim=true
    address = "0.0.0.0"
    port = 5001

    # We need to bump this because we run one stream per user +
    # polling for domain connections which can add up quite a bit
    # once you're around ~100 users.
    rate-limit.max-api-services-queue-size = 80000
    interactive-submission-service {
      enable-verbose-hashing = true
    }
  }

  http-ledger-api {
    port = 7575
    address = 0.0.0.0
    path-prefix = ${?CANTON_PARTICIPANT_JSON_API_SERVER_PATH_PREFIX}
  }

  parameters {
    initial-protocol-version = 34
    # tune the synchronisation protocols contract store cache
    caching {
      contract-store {
        maximum-size = 1000 # default 1e6
        expire-after-access = 120s # default 10 minutes
      }
    }
    # Bump ACS pruning interval to make sure ACS snapshots are available for longer
    journal-garbage-collection-delay = 24h
  }

  # TODO(DACH-NY/canton-network-node#8331) Tune cache sizes
  # from https://docs.daml.com/2.8.0/canton/usermanual/performance.html#configuration
  # tune caching configs of the ledger api server
  ledger-api {
    index-service {
      max-contract-state-cache-size = 1000 # default 1e4
      max-contract-key-state-cache-size = 1000 # default 1e4

      # The in-memory fan-out will serve the transaction streams from memory as they are finalized, rather than
      # using the database. Therefore, you should choose this buffer to be large enough such that the likeliness of
      # applications having to stream transactions from the database is low. Generally, having a 10s buffer is
      # sensible. Therefore, if you expect e.g. a throughput of 20 tx/s, then setting this number to 200 is sensible.
      # The default setting assumes 100 tx/s.
      max-transactions-in-memory-fan-out-buffer-size = 200 # default 1000
    }
    # Restrict the command submission rate (mainly for SV participants, since they are granted unlimited traffic)
    command-service.max-commands-in-flight = 30 # default = 256
  }

  monitoring.http-health-server {
    address="0.0.0.0"
    port=7000
  }

  topology.broadcast-batch-size = 1
}

# Sequencer
canton.sequencers.sequencer {
  init {
    generate-topology-transactions-and-keys = false
    identity.type = manual
  }

  storage = ${_storage} {
    config.properties {
      databaseName = "sequencer"
      currentSchema = "sequencer"
    }
  }

  public-api {
    address = "0.0.0.0"
    port = 5008
  }

  admin-api {
    address = "0.0.0.0"
    port = 5009
  }

  monitoring.grpc-health-server {
    address = "0.0.0.0"
    port = 5062
  }

  sequencer {
    config {
      storage = ${_storage} {
        config.properties {
          databaseName = "sequencer"
          currentSchema = "sequencer_driver"
        }
      }
    }
    type = reference
  }
}

# Mediator
canton.mediators.mediator {
  init {
    generate-topology-transactions-and-keys = false
    identity.type = manual
  }

  storage = ${_storage} {
    config.properties {
      databaseName = "mediator"
      currentSchema = "mediator"
    }
  }

  admin-api {
    address = "0.0.0.0"
    port = 5007
  }

  monitoring.grpc-health-server {
    address = "0.0.0.0"
    port = 5061
  }
}

################
# Participants #
################

# SV
canton.participants.sv = ${_participant} {
  storage.config.properties.databaseName = participant-sv
  monitoring {
    http-health-server.port = ${CANTON_PARTICIPANT_HTTP_HEALTHCHECK_PORT_PREFIX}00
    grpc-health-server.port= ${CANTON_PARTICIPANT_GRPC_HEALTHCHECK_PORT_PREFIX}00
  }
  http-ledger-api.port = ${CANTON_PARTICIPANT_JSON_API_PORT_PREFIX}00
  admin-api.port = ${CANTON_PARTICIPANT_ADMIN_API_PORT_PREFIX}00

  ledger-api{
    port = ${CANTON_PARTICIPANT_LEDGER_API_PORT_PREFIX}00
    auth-services = [{
      type = unsafe-jwt-hmac-256
      target-audience = ${API_AUDIENCE}
      secret = "unsafe"
    }]

    user-management-service.additional-admin-user-id = ${API_USER_NAME}
  }
}

# Participant 1
canton.participants.participant1 = ${_participant} {
  storage.config.properties.databaseName = participant-participant1
  monitoring {
    http-health-server.port = ${CANTON_PARTICIPANT_HTTP_HEALTHCHECK_PORT_PREFIX}01
    grpc-health-server.port= ${CANTON_PARTICIPANT_GRPC_HEALTHCHECK_PORT_PREFIX}01
  }
  http-ledger-api.port = ${CANTON_PARTICIPANT_JSON_API_PORT_PREFIX}01
  admin-api.port = ${CANTON_PARTICIPANT_ADMIN_API_PORT_PREFIX}01

  ledger-api{
    port = ${CANTON_PARTICIPANT_LEDGER_API_PORT_PREFIX}01
    auth-services = [{
      type = unsafe-jwt-hmac-256
      target-audience = ${API_AUDIENCE}
      secret = "unsafe"
    }]

    user-management-service.additional-admin-user-id = ${API_USER_NAME}
  }
}

# Participant 2
canton.participants.participant2 = ${_participant} {
  storage.config.properties.databaseName = participant-participant2
  monitoring {
    http-health-server.port = ${CANTON_PARTICIPANT_HTTP_HEALTHCHECK_PORT_PREFIX}02
    grpc-health-server.port= ${CANTON_PARTICIPANT_GRPC_HEALTHCHECK_PORT_PREFIX}02
  }
  http-ledger-api.port = ${CANTON_PARTICIPANT_JSON_API_PORT_PREFIX}02
  admin-api.port = ${CANTON_PARTICIPANT_ADMIN_API_PORT_PREFIX}02

  ledger-api{
    port = ${CANTON_PARTICIPANT_LEDGER_API_PORT_PREFIX}02
    auth-services = [{
      type = unsafe-jwt-hmac-256
      target-audience = ${API_AUDIENCE}
      secret = "unsafe"
    }]

    user-management-service.additional-admin-user-id = ${API_USER_NAME}
  }
}


# Participant 3
canton.participants.participant3 = ${_participant} {
  storage.config.properties.databaseName = participant-participant3
  monitoring {
    http-health-server.port = ${CANTON_PARTICIPANT_HTTP_HEALTHCHECK_PORT_PREFIX}03
    grpc-health-server.port= ${CANTON_PARTICIPANT_GRPC_HEALTHCHECK_PORT_PREFIX}03
  }
  http-ledger-api.port = ${CANTON_PARTICIPANT_JSON_API_PORT_PREFIX}03
  admin-api.port = ${CANTON_PARTICIPANT_ADMIN_API_PORT_PREFIX}03

  ledger-api{
    port = ${CANTON_PARTICIPANT_LEDGER_API_PORT_PREFIX}03
    auth-services = [{
      type = unsafe-jwt-hmac-256
      target-audience = ${API_AUDIENCE}
      secret = "unsafe"
    }]

    user-management-service.additional-admin-user-id = ${API_USER_NAME}
  }
}


# Participant 4
canton.participants.participant4 = ${_participant} {
  storage.config.properties.databaseName = participant-participant4
  monitoring {
    http-health-server.port = ${CANTON_PARTICIPANT_HTTP_HEALTHCHECK_PORT_PREFIX}04
    grpc-health-server.port= ${CANTON_PARTICIPANT_GRPC_HEALTHCHECK_PORT_PREFIX}04
  }
  http-ledger-api.port = ${CANTON_PARTICIPANT_JSON_API_PORT_PREFIX}04
  admin-api.port = ${CANTON_PARTICIPANT_ADMIN_API_PORT_PREFIX}04

  ledger-api{
    port = ${CANTON_PARTICIPANT_LEDGER_API_PORT_PREFIX}04
    auth-services = [{
      type = unsafe-jwt-hmac-256
      target-audience = ${API_AUDIENCE}
      secret = "unsafe"
    }]

    user-management-service.additional-admin-user-id = ${API_USER_NAME}
  }
}


# Participant 5
canton.participants.participant5 = ${_participant} {
  storage.config.properties.databaseName = participant-participant5
  monitoring {
    http-health-server.port = ${CANTON_PARTICIPANT_HTTP_HEALTHCHECK_PORT_PREFIX}05
    grpc-health-server.port= ${CANTON_PARTICIPANT_GRPC_HEALTHCHECK_PORT_PREFIX}05
  }
  http-ledger-api.port = ${CANTON_PARTICIPANT_JSON_API_PORT_PREFIX}05
  admin-api.port = ${CANTON_PARTICIPANT_ADMIN_API_PORT_PREFIX}05

  ledger-api{
    port = ${CANTON_PARTICIPANT_LEDGER_API_PORT_PREFIX}05
    auth-services = [{
      type = unsafe-jwt-hmac-256
      target-audience = ${API_AUDIENCE}
      secret = "unsafe"
    }]

    user-management-service.additional-admin-user-id = ${API_USER_NAME}
  }
}
